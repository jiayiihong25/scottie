# Content extraction
pdfplumber
python-pptx

# Anki card generation
genanki

# Config / misc
pyyaml
python-dotenv

# OmniRoute (OpenAI-compatible HTTP gateway) calls
requests

# LangGraph (optional — pipeline runs sequentially without it)
langgraph
